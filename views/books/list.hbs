{{>nav}}
<div id='booksWrapperDiv'>

	{{#each books}}
		<div class='book'>
			<h3> {{title}}</h3>
			<h4>{{author}}</h4>
			<div>
				<a href='/books/{{_id}}'>Details</a>
				<a href='/books/{{_id}}/edit' class='orange'>Edit</a>
				<form action='/books/{{_id}}/delete' method='POST'>
					<button class='red'>Delete</button>
				</form>

			</div>
		</div>
	{{/each}}

</div>

<div class='anchor'>
	<a href='/'>Go Back</a>
</div>
